# Email Configuration
# Copy this file to .env and fill in your values
# The .env file is gitignored and will not be committed

# Gmail SMTP Configuration (optional)
# If these are set, emails will be sent via Gmail SMTP
# If not set, emails will be printed to the console (development mode)
#
# For Gmail, you need an App Password (not your regular password):
# 1. Go to https://myaccount.google.com/apppasswords
# 2. Generate an app password for "Mail"
# 3. Use that password here
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Incident notification recipients (comma-separated)
# These addresses receive email alerts when server errors (500s) occur
# INCIDENT_NOTIFICATION_RECIPIENTS=dev@example.com,oncall@example.com
